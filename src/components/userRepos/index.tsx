import {
    Container,
    NumberContainer
} from './styles'

const UserRepo = (props: any) => (
    <Container>
        <NumberContainer>
            <h1>20 Repositories</h1>
        </NumberContainer>
    </Container>
);

export default UserRepo;